- mainapp.c: Why set targets with audio_test_set_targets(new_freq, new_amp) if we audio_test_start(new_freq, new_amp)
- Format: See why we're still even fetchign the file server data
- Files: Renmae format_storage to files_format_storage()
- Audio_test: 500ms wait seems unrealiable
- Audio_test: Drop delay on unmute?
- Audio: Add strict asserts (e.g. unitilized etc.)
- Audo: See if unmute delay really necessary
- Audio + File: Move clamp percent to some common file
- GPIOS: Add lazy init?

- Fix reset settings button
- Recovery: Add margins on phone screen
- Recovery: Add glyphs
- Ensure log console always same size.
- Validate mp3 by magic
- Check out i2s related legacy warnings
- Fix flash_ota.sh always returning with an error
- Add Boot sound